# Agent Examples

Tracked example agents live in this directory so they do not become active runtime prompts.

To enable one locally, copy it into `.claude/agents/` with the same filename:

```bash
cp .claude/examples/agents/builder.md .claude/agents/builder.md
```

Only files under `.claude/agents/` are loaded by the Discord bridge.
