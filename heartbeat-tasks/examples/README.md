# Heartbeat Examples

Tracked example heartbeat configs live here so the active `heartbeat-tasks/*.json` runtime path can remain local-only.

To enable one locally, copy it into `heartbeat-tasks/`:

```bash
cp heartbeat-tasks/examples/projects.json heartbeat-tasks/projects.json
```
